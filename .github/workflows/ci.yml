name: IAM Automation CI Pipeline

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build-and-test:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Validate Python Scripts
        run: |
          python -m py_compile scripts/create_auth0_user.py
          python -m py_compile scripts/assign_role.py
          python -m py_compile scripts/onboard_user.py
          python -m py_compile scripts/enforce_mfa.py

      - name: CI Pipeline Success
        run: echo "IAM Automation CI Pipeline completed successfully."